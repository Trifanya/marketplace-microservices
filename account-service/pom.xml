<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>dev.trifonov.marketplace</groupId>
    <artifactId>account-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>account-service</name>

    <modules>
        <module>api</module>
        <module>impl</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <lombok.version>1.18.34</lombok.version>
        <flyway.version>10.20.1</flyway.version>
        <spring-kafka.version>3.2.4</spring-kafka.version>
        <postgresql.version>42.7.4</postgresql.version>
        <order-service.version>0.0.1-SNAPSHOT</order-service.version>
        <catalog-service.version>0.0.1-SNAPSHOT</catalog-service.version>
        <feedback-service.version>0.0.1-SNAPSHOT</feedback-service.version>
        <user-service.version>0.0.1-SNAPSHOT</user-service.version>
        <account-service.version>0.0.1-SNAPSHOT</account-service.version>
        <loki-logback-appender.version>1.5.1</loki-logback-appender.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.github.loki4j</groupId>
            <artifactId>loki-logback-appender</artifactId>
            <version>${loki-logback-appender.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.trifonov.marketplace.account-service</groupId>
            <artifactId>api</artifactId>
            <version>${account-service.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.trifonov.marketplace.order-service</groupId>
            <artifactId>api</artifactId>
            <version>${order-service.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.trifonov.marketplace.catalog-service</groupId>
            <artifactId>api</artifactId>
            <version>${catalog-service.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.trifonov.marketplace.feedback-service</groupId>
            <artifactId>api</artifactId>
            <version>${feedback-service.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.trifonov.marketplace.user-service</groupId>
            <artifactId>api</artifactId>
            <version>${user-service.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
            <version>${spring-kafka.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${flyway.version}</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>